name: continuous integration

on: [push]

jobs: 
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "hi vinay"
      - name: multi line script
        run: |
          node -v
          npm -v
          npm -v
          
          
          #comment
